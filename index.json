---
# Do not delete this header. Jekyll needs it so the template is processed.
---
[
{% for snippet in site.snippets %}
	{
		"title": "{{ snippet.title | jsonify }}",
		"filename": "{{ snippet.url | relative_url }}"
	}{% unless forloop.last %},{% endunless %}
{% endfor %}
]
